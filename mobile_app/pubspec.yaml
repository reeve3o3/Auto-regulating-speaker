name: auto_regulating_speaker
description: "音幻追蹤 - Auto-regulating Speaker System with UWB positioning technology"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.6.1


dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.2
  volume_controller: ^2.0.7
  http: ^1.1.0


  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0

flutter:


  uses-material-design: true

  assets:
    - lib/icons/
