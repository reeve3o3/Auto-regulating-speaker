name: uwb_control_app
description: 音幻追蹤 - UWB音響控制系統移動應用程式

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP 請求
  http: ^1.1.0
  
  # 本地存儲
  shared_preferences: ^2.2.2
  
  # UI 組件
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # 添加資源文件
  assets:
    - lib/icons/